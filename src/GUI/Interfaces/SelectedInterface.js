(function() {

    var SelectedInterface = {};
    SelectedInterface.$interface = 'SelectedInterface';

    SelectedInterface.setData = 1;

    SelectedInterface.setSelected = 1;

    SelectedInterface.isSelected = 0;

    package('COM.GUI.Interfaces.SelectedInterface', SelectedInterface);
})();
